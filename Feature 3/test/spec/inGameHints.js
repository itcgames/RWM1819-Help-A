/* global in-GameHints, describe, it, expect, should */

describe('inGameHints()', function () {
  'use strict';

  it('exists', function () {
    expect(inGameHints).to.be.a('function');

  });

  // Add more assertions here
});
