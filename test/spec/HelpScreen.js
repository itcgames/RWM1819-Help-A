/* global HelpScreen, describe, it, expect, should */

describe('HelpScreen()', function () {
  'use strict';

  it('exists', function () {
    expect(HelpScreen).to.be.a('function');

  });

  // Add more assertions here
});
